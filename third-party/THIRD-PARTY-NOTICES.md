# Third-party notices

## SQLite

`sqlite3.dll` is built from the SQLite project. SQLite source code is in the public domain. See the official SQLite licensing page for details:

<https://www.sqlite.org/copyright.html>

The migration tool uses SQLite only to update the local Codex state database during list reconstruction.
